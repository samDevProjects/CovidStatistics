{% extends 'base.html.twig' %}

{% block title %}Edit Departement{% endblock %}

{% block body %}
<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-header">
                    <h5>Edit Departement</h5>
                    <a class="btn btn-info btn-left" href="{{ path('departement_index') }}">Back to list</a>
            </div>
            <div class="card-block table-border-style">

                {{ include('departement/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('departement/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
